# 操作日志（AlohaMini）

> 每次改动/操作按时间倒序记录：做了什么、为什么、结果。
> 与 `项目总览.md`「当前状态」和 `上游更新记录/` 互补：这里是流水账，那里是状态快照。

---

## 2026-06-24

### 建项目框架
- 在 umbrella 工作区 `F:\chengshenzhilu\Robot\AlohaMini\` 建文档骨架：`项目总览.md` / `复刻基线维护原则.md` / `操作日志.md`（本文件）/ `学习笔记.md` / `上游更新记录/`。
- 方法论移植自 `reBot-DevArm`，按 AlohaMini 无 submodule 的实际精简。
- umbrella 设计为独立「笔记/治理」git 仓：只跟踪自己的学习文档；3 个机器人仓在 `.gitignore` 中排除（各自独立 git）。
- 资料（2 个 PPTX + `3.jpg`）归入 `资料/`，二进制 gitignore、本地保留。

### 三仓只读克隆
- 决策：先 clone「只读起步」（不动 GitHub，先把代码/硬件资料拉到本地读），想锁定再 fork。fork 目标 = `fanhao375`（待用户确认后执行）。
- 克隆 3 仓到 umbrella 下（git 全局走代理 7890，`ls-remote` 已通）：

| 仓库 | 本地目录 | HEAD | 日期 | 大小 | origin |
|------|---------|------|------|------|--------|
| liyiteng/AlohaMini | `AlohaMini/` | `61246b6` feat: add URDF files for AlohaMini2 | 2026-06-18 | ~1.0GB | liyiteng |
| liyiteng/AM-ARM | `AM-ARM/` | `5b03af1` feat(urdf): add AM-ARM200 URDF model | 2026-06-18 | ~245MB | liyiteng |
| liyiteng/lerobot_alohamini | `lerobot_alohamini/` | `a85e4076` feat(recording): enhance dataset handling… | 2026-06-23 | ~288MB | liyiteng |

- 三仓均 main 分支、工作区干净。F 盘剩 140+GB。
- ⚠️ origin 仍指 liyiteng（未 fork）。`lerobot_alohamini` 是 `huggingface/lerobot` 的 fork（两层上游，未来配 upstream 时处理）。

### 资料盘点（克隆前）
- 读完两个 PPTX（产品介绍 v1.8 + 开源资料）+ `3.jpg`（两台实物）。
- 开源资料给出：新手教程 / 开箱指南（语雀）+ `github.com/liyiteng/lerobot_alohamini`。
- 据此 + GitHub 摸清三仓结构、BOM（舵机型号）、控制架构。详见 `学习笔记.md`。
