# 2026-06-24 首次克隆（基线建立）

**结论：三仓首次只读克隆到本地，origin 直指 liyiteng，尚未 fork，未配 upstream。**

## 📌 检查信息
- 检查时间：2026-06-24
- 走代理：git 全局 http.proxy = 127.0.0.1:7890（`ls-remote` 已通）
- 阶段：clone「只读起步」，fork 推迟到用户确认（目标账号 `fanhao375`）

## 📦 各仓库状态
| 仓库 | 本地 commit（克隆点） | 上游最新 | 新增提交数 | 是否同步 |
|------|---------------------|---------|-----------|---------|
| liyiteng/AlohaMini | `61246b6`（2026-06-18 add URDF for AlohaMini2） | 同 | 0（克隆即最新） | ✅ 克隆 |
| liyiteng/AM-ARM | `5b03af1`（2026-06-18 add AM-ARM200 URDF） | 同 | 0 | ✅ 克隆 |
| liyiteng/lerobot_alohamini | `a85e4076`（2026-06-23 enhance dataset handling） | 同 | 0 | ✅ 克隆 |

## 🔍 主要变化
- 首次建立基线，无差异分析。三仓均 main、工作区干净。

## ⏸️ 待办（remote 配置，需用户确认后执行）
- fork 三仓到 `fanhao375` → origin 改指 fork、liyiteng 设 `upstream`。
- `lerobot_alohamini` 是 `huggingface/lerobot` 的 fork：配两层上游（`upstream`=liyiteng、`upstream-hf`=huggingface/lerobot）。

## 📝 下次检查触发条件
- 决定 fork 锁定时
- 进入软件实操（装 lerobot_alohamini）前
- 知道官方有更新 / 大阶段前
